# Moxie — agent instructions

Read and follow [AGENTS.md](AGENTS.md) first. It is the single shared policy entry point; do not maintain a divergent second policy here. Follow its required reading, task contracts, legacy-source checks, common-API boundaries, owner gates and acceptance rules.

Before implementation, verify the actual writable repository root and read-only legacy snapshot, then identify the current milestone in [06-implementation-roadmap.md](docs/spec/06-implementation-roadmap.md). A historical model campaign or local handover cannot silently override the shared engine ownership policy.
