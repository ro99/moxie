[package]
name = "moxie-storage"
version = "0.0.0"
edition = "2024"

# Must be accepted: a storage crate reading its own artifact path is not
# machine telemetry. Guards the segment matcher against flagging every path.
