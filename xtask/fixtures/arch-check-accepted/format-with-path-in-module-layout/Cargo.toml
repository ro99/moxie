[package]
name = "moxie-format"
version = "0.0.0"
edition = "2024"

# Must be accepted: `#[path]` inside an ordinary module doing only pure
# work. Guards the base split against rejecting the combination itself.
