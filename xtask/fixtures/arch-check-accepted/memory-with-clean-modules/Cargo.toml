[package]
name = "moxie-memory"
version = "0.0.0"
edition = "2024"

# Must be accepted: nested modules doing exactly what the ledger does --
# integer accounting over declared byte counts, no filesystem, no model name.
