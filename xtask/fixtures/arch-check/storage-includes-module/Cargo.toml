[package]
name = "moxie-storage"
version = "0.0.0"
edition = "2024"

# Must be rejected: same mixed chain on the storage side -- an include into
# a module that names a model family.

[package.metadata.moxie-arch-check]
expect-rule = "storage interprets model metadata"
