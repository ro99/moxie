[package]
name = "moxie-storage"
version = "0.0.0"
edition = "2024"

# Must be rejected: the model-family breach arrives by `include!`. Same
# treatment as the format side: follow and inspect, don't ban includes.

[package.metadata.moxie-arch-check]
expect-rule = "storage interprets model metadata"
