[package]
name = "moxie-storage"
version = "0.0.0"
edition = "2024"

# Must be rejected: the sys half of the same bypass. root.join("sys/fs/cgroup")
# is the sensor's spelling for the cgroup hierarchy.

[package.metadata.moxie-arch-check]
expect-rule = "machine telemetry outside moxie-host"
