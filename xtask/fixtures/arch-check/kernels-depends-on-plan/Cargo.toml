[package]
name = "moxie-kernels"
version = "0.0.0"
edition = "2024"

[package.metadata.moxie-arch-check]
expect-rule = "forbidden dependency"

[dependencies]
moxie-plan = { path = "../../../../crates/moxie-plan" }
