[package]
name = "moxie-storage"
version = "0.0.0"
edition = "2024"

# Must be rejected: same `#[path]` context rule on the storage side.

[package.metadata.moxie-arch-check]
expect-rule = "storage interprets model metadata"
