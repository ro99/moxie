[package]
name = "moxie-storage"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
moxie-types.workspace = true
moxie-format.workspace = true

[dev-dependencies]
# Test-only, for reading an artifact's `config.json` and checking that the
# packing parameters an importer is handed are the ones the artifact declares.
# `arch-check` excludes dev-dependencies, because document 02 permits a test
# harness there.
serde_json = "1"

[lints]
workspace = true
