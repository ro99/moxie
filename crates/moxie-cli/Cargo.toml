[package]
name = "moxie-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "moxie"
path = "src/main.rs"

[dependencies]
moxie-engine.workspace = true
moxie-graph.workspace = true
moxie-types.workspace = true
moxie-memory.workspace = true
moxie-host.workspace = true
moxie-oracles.workspace = true
moxie-model-api.workspace = true
moxie-models.workspace = true
moxie-format.workspace = true
moxie-storage.workspace = true

[dev-dependencies]
moxie-interp.workspace = true
moxie-state.workspace = true
# Task 0056's temporary host tensor-parallel harness reads the lowering.
moxie-plan.workspace = true
# Test-only, for reading a checkpoint's `config.json` and comparing it against
# the geometry a model module declares. `arch-check` excludes dev-dependencies
# in every table, because document 02 permits a test harness there.
serde_json = "1"

[lints]
workspace = true
