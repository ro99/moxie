# Handover — M4 closure ledger (coordinator working record)

Status: **M4.2 active.** This is the coordinator's running ledger for M4
(coordinator.md §2), not a task record and not a closure. Update at meaningful
transitions.

## Workspace identity

- Writable root `/home/rodrigo/Developer/moxie`, branch `main`, base
  `5f9b6c9` (clean at ledger open, 2026-09-20).
- Read-only legacy root `/home/rodrigo/Developer/strata` at
  `2dc566eb8e440fff4837ac75ca1dad1b20c2264e`.
- `/models` and `/fast/models` read-only checkpoint roots (ADR 0020).
  `/fast/models/cyankiwi/GLM-5.2-AWQ-INT4` (commit
  `6e4f5c19d96e79e705d9918f763e132059dc668e`) is newly present as of this
  session — see [checkpoint-inventory.md](../evidence/checkpoint-inventory.md)'s
  2026-09-20 update. Not an O1 catalog revision; metadata-only use.
- Team per coordinator.md's team table: builder Codex `luna`
  (`gpt-5.6-luna`, max effort), independent reviewer Codex `sol`
  (`gpt-5.6-sol`, high effort, read-only), coordinator Claude Opus. Both
  started this session in their own Herdr tabs (`wC:t9` luna, `wC:tA` sol).
  Both panes reported "less than 10% of your 5h [Codex] usage limit left" at
  start — a shared account-level warning, not per-agent; flagged to the
  owner, not yet acted on. If either worker stalls mid-task on a usage-limit
  message, that is the cause to check first, not a task defect.

## Milestone obligations (roadmap M4, original item numbers)

| Item | Outcome | State | Owner | Dependency | Evidence | Next action |
|---|---|---|---|---|---|---|
| M4.1 | Common paged device attention/prefill/append, complete device state | **Accepted** (owner, 2026-09-20) | closed | none | [task 0037](../tasks/0037-m4-paged-device-attention.md), [task 0038](../tasks/0038-m4-device-kv-state-authority.md) | none — closed |
| M4.2 | Shared MLA/state descriptors, GLM-5.2 mathematical fixtures; absorbed path only after mask/projection/rounding validation | **Accepted** (owner, 2026-09-21) | luna (build) / sol (review) / coordinator | none technical | [task 0039](../tasks/0039-m4-mla-state-descriptors-and-glm52-fixtures.md) **accepted**: descriptor + FP64 oracle, 2 review rounds, round 1 found 2 blocking RoPE-rotation defects, repaired and re-reviewed clean. [Task 0040](../tasks/0040-m4-mla-reference-plan-admission.md) **accepted**: reference MLA plan admission + host execution, 2 review rounds, round 1 found an untested (but correct) attention-score-scale formula, repaired and re-reviewed clean. Both: all host gates pass; no GPU, no absorption, no DSA | none — closed. Device MLA execution and the absorbed/fused path are unopened successor work, not automatically authorized by this closure |
| M4.3 | Bounded host-backed state/page streaming, online-softmax merge | **Substantively complete for conventional attention** (owner, 2026-09-21) | luna (build) / sol (review) / coordinator | none technical | [task 0041](../tasks/0041-m4-host-backed-page-streaming.md), [task 0042](../tasks/0042-m4-n-block-host-backed-streaming.md), [task 0043](../tasks/0043-m4-host-backed-streaming-plan-admission.md) all **accepted**: bounded N-block mechanism, generalization, and graph admission, each round-1-caught-a-real-defect. Proven on both SM86 GPUs and SM120 | none required. MLA streaming and prefetch/overlap (M6.3) are explicit non-goals, not gaps |
| M4.4 | COW forks, recurrent/convolution/index-state snapshot/replay | **Accepted in full** (owner, 2026-09-21) | luna (build) / sol (review) / coordinator | none technical | [0044](../tasks/0044-m4-cow-paged-fork.md), [0045](../tasks/0045-m4-device-cow-fork.md), [0046](../tasks/0046-m4-recurrent-state-snapshot-replay.md), [0047](../tasks/0047-m4-convolution-history-snapshot-replay.md), [0048](../tasks/0048-m4-sparse-index-snapshot-replay.md) all **accepted**: host+device paged KV COW fork, and model-independent snapshot/replay stores for all three remaining `RestoreCapability::Explicit` kinds (recurrent, convolution, sparse index), each a genuinely distinct irreversibility shape, none implementing real model math | none required for M4.4's own text. N-branch COW generalization is named out of scope by tasks 0044/0045 and remains separately unopened, not owed by this closure |
| M4.5 | Same-user prefix reuse and continuation; correct checkpoint/config identity; a full prefix hit produces valid next-token logits or recomputes the required boundary step | **Not started — next M4 deliverable** | unassigned | M4.1's device state (met); document 04's logit-validity distinction (committed-in-history vs. materialized-forward-position) governs the contract | none | no task opened yet, per explicit instruction not to start work. Read document 04 line 35 and task 0038's device state authority before scoping the first bounded task |
| M4.5 | Same-user prefix reuse and continuation | Not started | unassigned | M4.1's device state (met) | none | none yet |

Task 0039 deliberately covers only M4.2's first half (descriptor + oracle).
The roadmap's own text splits M4.2's second half — absorbed/layout-specific
paths — behind "mask, projection and rounding validation," so it is named
here as M4.2's own remaining obligation, not silently folded into 0039's
acceptance.

## Work queue (critical path)

1. Task 0039 (this session): MLA/state descriptor + GLM-5.2 FP64 oracle,
   validated on synthetic shapes drawn from the real checkpoint's config.
   Removes the dependency every later M4.2/M4.3 MLA-shaped decision needs: an
   independent oracle to check against.
2. M4.2's second half (successor task, not yet numbered): admit
   `Op::MlaAttention` through `moxie-plan`/`moxie-state` as a reference
   (unabsorbed) path, gated on task 0039's oracle passing review. Absorbed
   fusion stays out of scope until that reference path is validated, per the
   roadmap line itself.
3. M4.3–M4.5 remain unstarted and are not blocked on each other; M4.5 could
   proceed independently of M4.2 if capacity allows, since it depends on
   M4.1 (met) and not on MLA. Not queued this session — M4.2 is the owner's
   named next deliverable.

## Decisions

None yet this ledger. `coordinator.md` amended 2026-09-20 (this session) to
record the exact `-c model=`/`-c model_reasoning_effort=` start flags Herdr
agent starts need — a plain `codex --yolo` inherits whatever model
`~/.codex/config.toml` last left selected, which produced two `sol`-model
panes on first start this session.

## Remaining hypotheses and blockers

None blocking task 0039's start. The Codex 5h usage-limit warning on both
panes (see Workspace identity) is a watch item, not yet a blocker.

## Next task

Assign task 0039 to `luna` under `/ponytail:ponytail`, per coordinator.md §4.
