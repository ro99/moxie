# Handover — close M1.3, then begin M1.4

## Workspace identity

- Writable repository: `/home/rodrigo/Developer/moxie`, branch `main`.
- Task 0012 implementation is committed locally at `6305f9d`, its first review-readiness record at
  `df9b9f4`, and all three owner-review corrections at `eaf8846`; this correction record follows in
  the current worktree. The receiving reviewer must confirm the current commit and dirty state
  before editing and preserve unrelated changes. Nothing has been pushed by this implementation run.
- Read-only legacy reference: `/home/rodrigo/Developer/strata` at
  `2dc566eb8e440fff4837ac75ca1dad1b20c2264e`.

## Completed facts

M1.3's resource ledger, measured host/device capacity, rank-owned CUDA context, event-backed leases,
basic admitted device arena and admitted graph/resource plan are accepted in tasks 0006–0011.
Tasks 0008 and 0009 now carry their previously missing accepted status in their own headers.

Task 0012 now selects and executes the exact synthetic BF16
`Linear -> RmsNorm -> Residual` chain on one admitted allocation and one stream, with no
intermediate device-to-host transfer. Aggregate and per-SM qualification pass 39/39 on all three
UUIDs; driver faults, cancellation, exact admission, independent bounds and Compute Sanitizer gates
also pass. Owner-review corrections make RMS intermediate overflow a typed numerical failure,
account for the final output's overlap with retained sources, and preserve immutable weight bindings
after completed cancellation recovery. The affected gates were rerun. The task result records the
precise commands, measurements and limitations.

## Decisions

[Task 0012](../tasks/0012-m1-selected-bf16-device-chain.md) is the final planned M1.3 assignment.
It owns shared kernel selection, exact workspace admission, the BF16
`Linear -> RmsNorm -> Residual` device chain, event-retained operands and independent numerical
validation on the required GPU architectures.

Implementation defects found during owner review are task 0012 corrections. They do not justify
another preparatory M1.3 task. A new M1.3 assignment is permitted only when evidence demonstrates a
defect outside task 0012 in scope that was already accepted.

## Remaining hypotheses and blockers

Task 0012 is implemented and its first owner-review findings are corrected, but it awaits owner
re-review/acceptance. The review range begins after the pre-implementation base `9e529d7` and now
includes implementation commit `6305f9d`, first review-readiness commit `df9b9f4`, correction commit
`eaf8846` and this living-record update. The owner review considered the `moxie-interp` stateless
trace API reasonable to accept: it reuses the existing dispatcher/oracles so the required every-node
comparison is possible, and no equation changed. Checkpoint/model execution, attention/state
integration, actual context, quality and performance are outside this closure task.

M1.4 is not complete. Task 0004 accepted the host transaction mechanism only. Appendable paged
state, sampler history and deterministic greedy/temperature distribution tests, the generation
service and the diagnostic CLI remain roadmap requirements.

## Next task

The owner should re-review [task 0012](../tasks/0012-m1-selected-bf16-device-chain.md), especially
correction commit `eaf8846`; the complete source range is `9e529d7..eaf8846`. Any further
corrections stay in task 0012. Once the owner accepts the result, update task 0012 and the support
matrix to state **M1.3 complete**.

The first implementation assignment after that acceptance must be M1.4 appendable paged-state
allocation bound to [task 0004's](../tasks/0004-m1-state-transactions.md) accepted transaction
mechanism. The following M1.4 slices may integrate sampler history, the generation service and the
minimal diagnostic CLI. Do not define task 0013 as another M1.3 prerequisite.
