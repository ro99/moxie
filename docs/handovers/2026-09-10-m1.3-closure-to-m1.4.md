# Handover — M1.3 complete; M1.4 active

## Workspace identity

- Writable repository: `/home/rodrigo/Developer/moxie`, branch `main`.
- Accepted implementation `6305f9d`, corrections `eaf8846` and `1138a2a`, reviewed evidence
  through `ce7548d`. This acceptance record follows those commits.
- The owner authorized documenting acceptance and pushing to `origin/main`. Confirm current
  commit and dirty state before new work; preserve unrelated changes.
- Read-only legacy reference: `/home/rodrigo/Developer/strata` at
  `2dc566eb8e440fff4837ac75ca1dad1b20c2264e`.

## Completed facts

The owner accepted task 0012 on 2026-09-10 after independent re-review found no remaining blockers.
**M1.3 complete; M1.4 active.** Tasks 0006–0011 already accepted the ledger, measured capacity,
rank-owned context, event-backed leases, basic arena and admitted graph/resource plan.
Task 0012 adds selected synthetic BF16 Linear/RmsNorm/Residual execution in one admitted allocation
on one stream, without intermediate device-to-host transfers.

All five review findings are resolved: RMS overflow, overlapping host-output admission, immutable
weight recovery after cancellation, persistent fatal-readback loss and explicit RMS-underflow
refusal. Exact zeros and small-normal inputs pass independent probes. Conservative refusal of
unqualified underflow is accepted for this bounded slice.

Independent final verification passed 556 host tests + 8 doctests, 572 device-feature tests +
11 doctests, aggregate GPU qualification 39/39 and extra numerical boundary probes on every GPU,
memcheck with zero errors on all three UUIDs, SM120 racecheck/initcheck with zero hazards/errors,
and clippy, architecture, specification and formatting. Separate per-profile and isolated no-driver
lanes were not repeated in the final independent review; their prior evidence remains applicable.
Task 0012 preserves the detailed correction history and negative experiments.

## Decisions

Task 0012 closes M1.3. Do not add another preparatory M1.3 task. A new M1.3 assignment requires a
demonstrated defect in accepted scope. The stateless interpreter trace addition is accepted.
No numerical bound was loosened.

## Remaining hypotheses and blockers

No task 0012 acceptance blocker remains. M1.4 is active, not complete: task 0004 accepted only the
host transaction mechanism. Appendable paged state, sampler history and deterministic
greedy/temperature tests, generation service and diagnostic CLI remain requirements.
Checkpoint/model execution, attention integration, actual context, model quality and performance
are not established by M1.3 acceptance.

## Next task

Define the next bounded task contract before implementation: **M1.4 appendable paged state bound
to [task 0004's](../tasks/0004-m1-state-transactions.md) accepted transaction mechanism**.
Extend the existing memory authority and transaction ownership; the new slice must name its
resource, cancellation and acceptance contracts. Subsequent M1.4 slices may integrate sampler
history, generation service and diagnostic CLI. Task 0013 must not be another M1.3 prerequisite.
