# Handover — M1.4 complete; M1.5 active

## Workspace identity

- Writable repository: `/home/rodrigo/Developer/moxie`, branch `main`.
- Accepted task 0015 implementation `f7cff56`, corrections `5990f2a` and
  `7a08adf`, final reviewed record `3f13784`. This acceptance record follows
  those commits.
- The owner authorized documenting acceptance and pushing to `origin/main`.
- Read-only legacy reference: `/home/rodrigo/Developer/strata` at
  `2dc566eb8e440fff4837ac75ca1dad1b20c2264e`; its existing untracked `.pi/` and
  `tests/p2p/` remain untouched.
- Local checkpoint roots `/models` and `/fast/models` remain read-only inputs.
  No checkpoint data was copied, converted, deleted or modified while closing M1.4.

## Completed facts

The owner accepted [task 0015](../tasks/0015-m1-generation-service-diagnostic-cli.md)
on 2026-09-11 after final independent review found no remaining blocker.
**M1.4 is complete; M1.5 is active.** Accepted M1.4 work now includes task 0004's
shared sequence transactions, task 0013's admitted physical paged state, task
0014's transactional base sampler history/distributions, and task 0015's shared
generation service and minimal diagnostic CLI.

Task 0015's final independent gates passed 593 host tests + 9 doctests, 609
device-feature tests + 12 doctests, 39/39 real GPU regression cases, 71 rejecting
and 20 accepted architecture fixtures, both clippy lanes, formatting and
specification. The final rollback probe confirmed that preparation failure clears
tentative rows, live logits and the open transaction, retry succeeds, and a foreign
transaction ID does not mutate local work. Reviewer evidence is retained under
`results/task0015-final-review-2026-09-11/`; its checked `SHA256SUMS` manifest hash
is `a605e5dbae42d20e2a5c16279b3461806073a58d9cb8a900ee62aabbe57ae11b`.

M1.4 acceptance is bounded to synthetic host-reference generation with requested
context <=256. It establishes transaction, paging, sampling and service ownership;
it does not establish checkpoint/model execution, device attention, 32,768-token
inference, model quality or product performance.

## Decisions

Task 0015 and M1.4 are closed. Reopen them only for a demonstrated defect in their
accepted scope. No numerical threshold, precision, context target, compatibility
surface or owner gate changed. O1–O7 remain open. M4 still owns paged device
attention and actual long-context execution; M8 owns production HTTP/text surfaces
and the complete sampler pipeline.

## Remaining hypotheses and blockers

Roadmap M1.5 requires a reduced Gemma-like dense graph and an actual Gemma text
graph expressed only through shared operations. A shallow read-only inventory now
finds `/fast/models/cyankiwi/gemma-4-31B-it-AWQ-8bit` with `config.json`, a
safetensors index and seven named shards. This is discovery only: exact source
revision, tokenizer/template identity, index-to-shard completeness, tensor roles,
packing parameters and content hashes have not yet been recorded. The directory
name suggests an integer artifact whose importer and execution path may depend on
M3; no support or quality conclusion follows from its presence.

O1 still blocks declaring a release catalog/order, but it does not prevent a
bounded provisional stress graph. O2 and O5 block quality approval and any
requantization/conversion. If the inspected Gemma artifact cannot feed M1.5 without
an unimplemented integer importer or new mathematics, stop at that dependency;
do not add a private loader, dequantization fallback or model-owned operation.

## Next task

Author task 0016 as one bounded M1.5 Gemma graph/integration contract. Before
implementation, inspect the exact read-only Gemma artifact revision and shard
completeness, its tokenizer/template and tensor index, plus the pinned Gemma legacy
and upstream graph sources named by document 08. The contract must separate:

1. a reduced Gemma-like synthetic graph that exercises shared operations and may
   use synthetic BF16 weights;
2. the actual artifact graph and tensor-role mapping; and
3. any M3-blocked integer import/execution work that cannot be claimed by M1.5.

Use shared graph/oracle/state/service owners, add missing mathematics only as a
common semantic operation with an independent oracle, and require at least a
second distinct consumer. Stop before implementation if exact artifact identity or
shard completeness is unavailable, or if the proposed graph depends on an owner
ruling. No model support claim is allowed from the reduced graph alone.
