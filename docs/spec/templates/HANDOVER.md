# Handover — bounded continuation

## Workspace identity

Writable root, branch/commit, dirty files and their owners; read-only legacy root/commit; artifact and binary identities. Do not resolve worktree-relative source citations against the document's directory by assumption.

## Completed facts

Task/milestone gates passed, changed shared owner, consumers tested, commands/result IDs, deleted paths, support-matrix changes. Separate passed, failed and skipped lanes.

## Decisions

ADR IDs, owner rulings, defaults changed, explicit exception expiry. Link the normative contract, not only a conversation transcript.

## Remaining hypotheses and blockers

What is uncertain, evidence already checked, rejected approaches and why, missing artifacts/hardware/owner choice. Do not present estimated speedups as measurements.

## Next task

One bounded deliverable, owning component, required source reading, allowed files, oracle/tests, actual-context/resource gates and exact stop condition. No “finish model X however necessary.”
