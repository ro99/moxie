# Support matrix — claims must link to evidence

Generated by hand at M0. Document 07: "Release claims are generated from this matrix and its
passing gate IDs, not from the presence of flags." Every row's initial status is
NOT IMPLEMENTED, never supported, and a capability probe alone proves kernel availability, not
full-model quality or performance.

**Nothing in this repository executes a model.** Three rows now name a checkpoint, and none of them
is model support: task 0018 **imported** tensors that nothing runs, task 0020 **read** expert bytes
that nothing computed with, and task 0021 **computed** with a real layer's expert weights over
**synthetic activations and a route written by its test**. A layer that matches its own reference on
invented input says nothing about what the model would produce; that is O2, and it needs paired
output against the released model. The remaining gates below are toolchain, format and enforcement
gates. Reading any of them as model support would be exactly the substitution this matrix exists to
prevent.

## Gate IDs

[Task 0020](../tasks/0020-m2-weight-residency-authority.md) is **M2 item 2**,
**accepted on 2026-09-12** after seven rounds of independent review that found
**twenty-seven** issues — all reproduced, all fixed, none disputed; the seventh
recommended acceptance with no new blocking findings: one production weight-residency owner, connected to bounded
storage reads, a host cache, real device uploads, leases, eviction and
demand/prefetch classes.
Gates: 823 host tests + doctests, 843
device-feature tests, 39/39 real GPU cases, both clippy lanes, `spec-check`, and
`arch-check` passing every rule and fixture including the new
`a second weight-residency owner`. **It does not close M2**, whose exit still
needs a real out-of-device-memory working set that *executes*; nothing here
executes a routed layer, and the selected BF16 chain still refuses `Route`,
`ExpertMlp` and `Combine`.

[Task 0021](../tasks/0021-m2-expert-execution-plans.md) is **M2 item 3**,
**implemented on 2026-09-12 and awaiting independent review and owner
acceptance**: CPU expert fallback and GPU grouped candidate plans under one
interface, with an admitted envelope, a bounded queue that refuses rather than
waits, NUMA-placed host buffers and a reduction whose order is data.
Gates: 871 host tests, 894 device-feature tests, 42/42 real GPU cases, both
clippy lanes, `spec-check`, and `arch-check` with zero failures. **It does not
close M2**, whose item 4 is outstanding and whose exit asks for byte/cost traces
reconciled with the ledger across a whole working set rather than one layer.

[Task 0019](../tasks/0019-m2-routed-expert-semantics.md) opens **M2** with its
item 1 mathematics and was **accepted by the owner on 2026-09-12**, after three
rounds of independent review reporting ten findings — all reproduced, all fixed,
none disputed — the third finding no remaining blocker in the task's bounded
scope. Final gates: 727 host tests + 9 doctests, 743 device-feature tests + 12
doctests, 39/39 real GPU cases on sm_86 and sm_120, both clippy lanes, and
`arch-check` passing every rule and fixture. **The acceptance closes task 0019
only, not M2**, whose exit still needs a real out-of-device-memory working set
executing without OOM or hidden allocations.

Routed-expert semantics as shared operations. It adds
`Route`, `ExpertMlp` and `Combine` parameters with FP64 oracles and an
interpreter, composes the designated artifact's routed block in
`moxie_models::gemma4`, and adds a second synthetic MoE consumer carrying the
opposite value for every routing parameter. **It executes no checkpoint and
establishes no residency capability** — the memory authority, expert chunk
identity, demand cache, eviction and CPU fallback are M2 items 2 and 3 and
belong to task 0020. Its graphs are reduced synthetic fixtures over weights the
composition root invents; the designated artifact is 51.6 GB against a 24 GiB
largest device and nothing here makes it run. See
[the bring-up record](../models/gemma4.md).


[Task 0016](../tasks/0016-m1-gemma-reduced-graph.md) opens M1.5 and was
**accepted by the owner on 2026-09-12** after independent re-review. It was
corrected first: the review found two BF16 boundary omissions (the scaled
residual and the logit softcap), an unchecked extent product and an incomplete
acceptance claim. All four are fixed and the gate below reflects the corrected
code. Acceptance covers the reduced synthetic graph only and **does not close
M1.5**. It makes seven
family-mathematics parameters explicit
([ADR 0012](../decisions/adr/0012-explicit-family-operation-parameters.md)),
adds `GeGlu` and a grouped RMSNorm with their oracles, decouples the paged row
width from the query-head count so grouped-query attention stores what it
actually has, and adds `moxie-models` with a `gemma4` module
([ADR 0013](../decisions/adr/0013-one-model-crate-with-family-modules.md)).
**It executes no checkpoint.** Its graph is a reduced synthetic Gemma-like
fixture over weights the composition root invents; the Gemma 4 artifact is INT8
`pack-quantized` and its importer is M3. See
[the bring-up record](../models/gemma4.md).

[Task 0015](../tasks/0015-m1-generation-service-diagnostic-cli.md), implementation
`f7cff56` after contract `2fbacec` with corrections `5990f2a` and `7a08adf`, adds
the shared host-reference generation service and diagnostic CLI. The owner accepted
it on 2026-09-11 after final independent review through `3f13784`. Final gates pass
593 host tests + 9 doctests, 609 device-feature tests + 12 doctests, both clippy
lanes, 71 rejecting + 20 accepted architecture fixtures, and 39/39 real GPU
regression cases. This acceptance closes M1.4 within the explicit synthetic
host-reference boundary; it establishes no checkpoint/model, device attention,
actual 32K context, quality or performance support.

[Task 0014](../tasks/0014-m1-transactional-sampler-history.md), implementation `23a7a40`
after contract `4054dd7`, adds shared base distributions/RNG and admitted history
bound to the existing paged transaction. The owner accepted it on 2026-09-11 after
independent review of `23a7a40` / `1e6f253`; the task record qualifies the layout
chronology and retains the independent evidence hash. Final gates:
**584 host tests + 9 doctests**, **600 device-feature tests + 12 doctests**, both
clippy lanes, **65 rejecting + 18 accepted architecture fixtures**, and **39/39**
real GPU regression cases after the owner repaired the driver mismatch. History
storage reaches 32,768 entries with zero token-path allocations and complete cleanup;
this is not model generation, attention context or GPU sampler qualification.

Task 0013 (`c14e32a`, contract `a80ff2c`) adds admitted host paging bound to the
existing sequence transactions and was accepted after independent review through
`0f39cf5`. `G-PAGED-HOST` and `G-PAGED-ALLOCATION` pass. After correction
`c9a4b33`, the host workspace passes **570 tests + 8 doctests**,
and architecture passes **61 rejecting + 16 accepted fixtures**. Device regressions
pass **586 tests + 11 doctests**, device clippy and **39/39** aggregate real GPU
cases on all three UUIDs. The task result distinguishes stored rows from attention
context and records raw-log/binary identities. Isolated host build passes with no
CUDA toolkit/driver linkage; per-SM/visibility/sanitizer lanes were not repeated.

Task 0012's second corrections at `1138a2a` extend `G-BF16-DEVICE-CHAIN` and `G-LEASE-FAULTS`:
fatal readback permanently retains the lease and reservation; unqualified RMS FP32 underflow
returns typed `Numerical`. Both owner underflow cases pass primitive and integrated refusal tests
on every UUID, with an additional isolated scaling test. Aggregate and both per-SM profiles remain
39/39, and the reduced chain passes memcheck on SM86/SM120 plus SM120 racecheck/initcheck.
The qualified RMS path conservatively refuses nonzero square, mean, scaling and normalized
intermediates that round to zero or FP32 subnormals. It does not claim successful execution for all
finite BF16 operands. The owner accepted task 0012 on 2026-09-10 after independent re-review:
**M1.3 complete; M1.4 active**. Independent evidence includes 556 host tests + 8 doctests,
572 device-feature tests + 11 doctests, aggregate qualification 39/39, additional numerical boundary
probes and clean memcheck on every UUID, and clean SM120 racecheck/initcheck. Separate per-profile
and isolated no-driver runs retain their previously recorded evidence.

| Gate | Command | Lane | Result 2026-09-10 |
|---|---|---|---|
| `G-HOST-FMT` | `cargo fmt --all -- --check` | host | passed |
| `G-HOST-CLIPPY` | `cargo clippy --workspace --all-targets --locked -- -D warnings` | host | passed |
| `G-DEVICE-CLIPPY` | the same with `--features moxie-cuda/driver,moxie-kernels/fatbin,moxie-executor/driver,xtask/cuda` | device build | passed; **new at [task 0007](../tasks/0007-m1-rank-context-and-measured-capacity.md)**, which found and fixed two pre-existing `undocumented_unsafe_blocks` findings the lane had never been run against; extended at [task 0009](../tasks/0009-m1-event-backed-leases.md) with `moxie-executor/driver` |
| `G-HOST-TEST` | `cargo test --workspace --all-targets --locked --offline`; `cargo test --workspace --doc --locked --offline` | host | passed, 593 unit/integration tests + 9 doctests at accepted task 0015 |
| `G-HOST-ARCH` | `cargo xtask arch-check` | host | passed with **zero failures** at task 0020 — 78 negative + 21 accepted fixtures, 13 rules. The four failures every task since 0014 reported as "pre-existing" were review probe crates parked under `results/`, which `docs/README.md` declares ignored scratch. The walk now excludes root scratch **by reachability, computed exactly** — normalized members, globs expanded per segment, `{ workspace = true }` inheritance resolved through the same resolver the edge checker uses — and **fails closed**, checking everything, when reachability cannot be computed. **Four** review rounds found four different ways a cheaper approximation hid a production crate from every rule; the walk and the rules now share one dependency-table enumeration and one inheritance resolver, asserted to agree. Previously: 71 negative + 20 accepted fixtures, 12 rules; 343 generated module combinations checked against rustc by the host suite, each against all four crate boundaries |
| `G-HOST-SPEC` | `cargo xtask spec-check` | host | passed, 10 documents |
| `G-HOST-NODRIVER` | fresh target, `CUDA_HOME=/nonexistent NVCC=/nonexistent`, no CUDA on `PATH`; **`cargo build -p xtask` before `ldd`** | host | passed, 556 unit/integration tests; focused interpreter doctests passed separately. `ldd` shows no `libcuda`. The explicit build is load-bearing: without it `ldd` can read a device-lane artifact left in the shared `target/`, which it did once on 2026-09-08 ([toolchain.md](toolchain.md)) |
| `G-GPU-CAPACITY` | `cargo xtask-cuda capacity` | device | passed, **the host and** 3 devices measured and admitted against in one ledger; also passed with `CUDA_VISIBLE_DEVICES` reversed, each UUID keeping its own memory total |
| `G-GPU-SM86` | `cargo xtask-cuda test-gpu --profile sm86` | device | passed, RTX 3090 x2 |
| `G-GPU-SM120` | `cargo xtask-cuda test-gpu --profile sm120` | device | passed, RTX 5060 Ti |
| `G-LEASE-FAULTS` | `cargo test -p moxie-executor --features driver --test driver_faults -- --nocapture` | device | passed, actual allocation/copy/launch/event-query/event-sync/record/readback/free boundary faults for transient leases, device arenas, admitted resource plans and the selected chain on all 3 UUIDs; test-only symbol interposition |
| `G-GPU-ALL` | `cargo xtask-cuda test-gpu` | device | passed, 39 cases, both architectures qualified |
| `G-BF16-DEVICE-CHAIN` | `cargo xtask-cuda test-gpu`; SM86/SM120 profile runs; `compute-sanitizer` memcheck on each SM plus racecheck/initcheck | device | passed implementation evidence at `6305f9d` plus owner-review corrections at `eaf8846` and `1138a2a`, accepted by the owner on 2026-09-10; exact synthetic BF16 Linear/RmsNorm/Residual H8 rows1 and H17 rows5 chains on all 3 UUIDs, primitive bounds through H1024 rows64, typed RMS-overflow refusal, exact output-overlap admission and completed-cancellation weight recovery; sanitizer clean |
| `G-ARENA-HISTORY` | `cargo test -p moxie-memory --test arena_history` | host | passed; zero retained requested-heap growth after 100,000 variable-offset cycles with at most two live ranges |
| `G-ARENA-PENDING` | `cargo test -p moxie-executor --features driver --test device_arena -- --nocapture` | device | passed on every UUID; controlled pending work, cancellation, first-sweep withholding and second-sweep return of the actual range/source; ten-second watchdog fails closed |
| `G-RESOURCE-PLAN` | `cargo test -p moxie-executor --features driver --test resource_plan -- --nocapture` | device | passed on every UUID; exact graph/workload binding, activation slot identities and reuse, refusal before semantic execution, and physical/ledger reconciliation |
| `G-INTERP-BF16` | `cargo test -p moxie-interp` | host | passed, 19 unit + 28 acceptance tests + 3 compile-fail doctests ([task 0003](../tasks/0003-m1-bf16-reference-interpreter.md), [task 0004](../tasks/0004-m1-state-transactions.md), task 0012 stateless trace) |
| `G-PAGED-HOST` | `cargo test -p moxie-state -p moxie-memory --locked --offline` | host | passed, task 0013 plus correction `c9a4b33`; complete per-layer encoded rows across page boundaries, whole/chunk/later parity, partial acceptance/truncation, every cancellation boundary, sequence-unique transactions, exact post-admission lineage-exhaustion classification/cleanup, admission/cleanup and explicit unsupported forks |
| `G-PAGED-ALLOCATION` | `cargo test -p moxie-state --test paged_allocation --locked --offline -- --nocapture` | host | passed; 32,768 actual stored rows, zero append allocations, no retained growth after 10,000 aborts and zero charge after close; storage only. The admitted total moved at task 0017 because pages now belong to one layer rather than being shared, so the page table has one entry per (layer, page); the figures are in [task 0017](../tasks/0017-m4-per-layer-kv-geometry-and-window-eviction.md) |
| `G-WINDOW-ALLOCATION` | `cargo test -p moxie-state --test paged_window_allocation --locked --offline -- --nocapture` | host | task 0017; the same 32,768 actual stored rows with one layer windowed at 1,024. Admitted backing **206,360 B against 398,860 B** for full retention at the same context, zero append allocations while reclaiming, zero charge after close. **Storage capacity evidence only** — no attention runs and it is not a context claim |
| `G-SAMPLING-HOST` | `cargo test -p moxie-sampling -p moxie-state -p moxie-memory --locked --offline -- --nocapture` | host | passed, task 0014; exhaustive FP64 probabilities, Philox known answers and statistical bins, generated-only counts, pending materialization, partial acceptance/replay, every participant/commit cancellation boundary and foreign/stale refusal |
| `G-SAMPLING-ALLOCATION` | `cargo test -p moxie-state --test sampling_allocation --locked --offline -- --nocapture` | host | passed; 32,768 generated entries + 32,769 physical rows, 1,187,358 B admitted, zero token-path allocations, 10,000 aborts with no growth, exact allocation failures and complete cleanup |
| `G-GENERATION-HOST` | `cargo test -p moxie-cli --locked --offline --test generation` | host | accepted task 0015; exact dense/paged logits for two shapes, whole/chunk/tail/page and decode parity, fixed-seed CLI/service agreement, immutable paged-program ownership, row-shape admission, output provenance and cancellation/restart |
| `G-GENERATION-ALLOC` | `cargo test -p moxie-cli --locked --offline --test allocation -- --nocapture` | host | accepted task 0015; admitted peak bound, 1,000 cancellation/restart cycles, typed construction/forward failures, full service cleanup and direct whole-transaction rollback/retry when fallible paged preparation fails |
| `G-CT-IMPORT` | `cargo test -p moxie-format --locked --offline`; `cargo test -p moxie-storage --test gemma4_import --locked --offline -- --nocapture` | host | task 0018; compressed-tensors `pack-quantized` **import** to canonical affine form. All 256 INT8 and all 16 INT4 codes in every lane, group tails, per-channel and per-group granularity, BF16/F16/F32 scale dtypes read from the tensor header, and nonfinite/zero/negative scales plus truncated, overlapping, mis-sized and unknown-dtype headers each refused — every case against an independent oracle. Real artifact: 7 shards, 2008 tensors, 35,089,877,112 B payload re-derived, three modules imported reading 216,416,304 B. **Import is not execution and not model support** |
| `G-GEMMA-REDUCED` | `cargo test -p moxie-cli --locked --offline --test gemma` | host | task 0016, extended by task 0017; two reduced geometries generate through the shared service, dense/paged logits agree bit for bit across pages and decode, every one of the seven new parameters is proven load-bearing by substitution (task 0017 adds the global key/value head count and head dimension, which are now separate from the sliding ones), the softcap bounds every logit, the global layers carry no value projection while K still differs from V, a graph whose layers disagree on geometry is paged at each layer's own width, and the CLI agrees with the service. **Synthetic weights and reduced dimensions; not model support** |
| `G-KV-RETENTION` | `cargo test -p moxie-cli --locked --offline --test gemma reclaiming`; `cargo test -p moxie-state --test paged_window --locked --offline` | host | task 0017; **reclaiming a sliding layer's window changes no logit bit** against the full-retention dense reference, over two shapes and five page/chunk combinations, with the test asserting that reclamation actually occurred on every sliding layer and on no global one. Plus ring wrap at five page sizes, the tentative-headroom refusal, the two reclamation refusals with negative controls, and abort exactness on a **wrapped mixed-geometry** ring with a fault injected at all nine boundaries of the three publishing operations (append, sample staging, commit), restoring every readable row, both frontiers, the lineage, the committed sampler history and the ledger charge |
| `G-MOE-ROUTING-HOST` | `cargo test -p moxie-oracles --lib --locked --offline`; `cargo test -p moxie-interp --test reference_graphs --locked --offline`; `cargo test -p moxie-models --locked --offline`; `cargo test -p moxie-cli --locked --offline --test gemma` | host | task 0019; routed-expert **mathematics**. The router's four-stage score transform, the lower-id tie rule, renormalisation over the selected mass, and a per-expert scale applied after it and never renormalised away; grouped compute over the **fused** `[E, 2I, H]` / `[E, H, I]` tensors with the gate block ahead of the up block; and a combination whose reduction order is a parameter, tested on a fixture where FP32 addition is not associative. Each against an FP64 transcription written from the pinned reference: selection **exact**, coefficients and outputs inside counted `metric::bound` chains, whole-graph agreement over two shapes and both combination orders. Eight routed tensors proven load-bearing by substitution, plus the fused gate/up block order. Two consumers with opposite parameters throughout, both generating through the shared service with whole-versus-chunked prefill parity. **No residency, no expert cache, no chunk identity, no device kernel** — the selected BF16 chain refuses all three operations. Synthetic weights at reduced scale; **not MoE model support** |
| `G-RESIDENCY-HOST` | `cargo test -p moxie-memory --locked --offline`; `cargo test -p moxie-memory --test residency_allocation --locked --offline -- --nocapture`; `cargo test -p moxie-executor --test residency_reads --locked --offline -- --nocapture`; `cargo test -p moxie-engine --locked --offline` | host | task 0020; **weight residency, decided and read**. Document 03's chunk identity `(artifact, tensor/expert, logical range, format version)` with its separate prepared-layout identity; the lifecycle with explicit failure and cancellation transitions; `N` concurrent acquires of one chunk producing exactly **one** read; deterministic demand LRU with a bounded prefetch class that is queued behind demand, evicted first, and refused rather than admitted at demand data's expense; ADR 0009's conditional-memory floor; and an admission report naming the incoming chunk and its victims **before** one is removed. All nine of M2 item 5's cases, plus a named regression for each of three review rounds' twenty findings **and an exhaustive transition sweep**: 800 combinations of destination, urgency, joiner, ending, surviving lease, cache pressure and retirement, with the authority's structural invariants checked after every single operation and a harness that performs only work the scheduler actually handed out. The sweep found a defect three review rounds had missed, It reports its own coverage (800 combinations, 800 applying their ending, 0 short-circuited) and **catches ten of eleven deliberate mutations** of the authority; the eleventh is caught by its named regression. It complements the named tests rather than replacing them. The oracle is byte identity against an independent read, on hits, on post-eviction reloads and for coalesced waiters. **A nonblocking `acquire` is necessary but not sufficient for deadlock freedom** — the review found a cycle between the demand counter and the prefetch gate; it is fixed and tested. Control memory is a **bound**, not an estimate: the chunk identity is stored once, its length declared and enforced per acquire, and a gate measures retained heap against the admitted envelope at that bound. Allocation: **zero** heap allocations on a resident acquire and on release, zero retained growth over 10,000 demand/evict and 10,000 failed-read cycles, and authority/arena/ledger reconciling exactly after every case. Real artifact: nine distinct experts of `/fast/models/google/gemma-4-26B-A4B-it` layer 0, **107,053,056 B** read once each against a four-expert cache, every range verified independently. **Reading is not executing.** Nothing computes with those bytes, no checkpoint runs, and nothing was written under any checkpoint root |
| `G-RESIDENCY-DEVICE` | `cargo test -p moxie-executor --features driver --test residency_device -- --nocapture` | device | task 0020; the upload path on **all three UUIDs**. One real allocation per device backing the authority's ranges, with the cache's capacity **admitted from the ledger before a byte of it exists**, and the allocation bound to a one-per-scope entitlement the authority hands out. Two defects were found here and both are fixed: a device cache with an arena and no reservation at all (a placement simulator), and — by the independent review, measured on a 3090 — **8 MiB of allocation live against a 4 MiB charge**, with `close` releasing that charge while both allocations stayed readable. A chunk is unreadable until its copy is **observed**, its host source is pinned for the copy's lifetime, and every arrived range is verified by reading it back off the card. The all-entries-leased refusal is demonstrated against real device memory: immediate, typed, `leased_bytes == cap_bytes`, and the authority usable afterwards. **No kernel, no model, no checkpoint** |
| `G-EXPERT-PLAN-HOST` | `cargo test -p moxie-plan --locked --offline -- --nocapture`; `cargo test -p moxie-kernels --locked --offline`; `cargo test -p moxie-executor --test grouped_experts --locked --offline -- --nocapture`; `cargo test -p moxie-host --test numa_topology --locked --offline` | host | task 0021; **expert execution plans, and the host candidate**. One interface over a grouped-device and a tiled-host candidate: the choice is `bytes_per_row = transfer / reuse` against a **declared** policy parameter that is reported with every decision and is explicitly **not** a measured crossover; `required` on one candidate excludes the other and errors rather than falling back; the queue is bounded, refuses rather than grows or waits, and an acquire the residency authority refuses while it holds work is backpressure. The host kernel is **bitwise** equal to task 0019's oracle for both gate transforms, with the tile width proven numerically inert across six widths, and the reduction is an explicit per-row permutation so a mixed plan cannot depend on completion order — asserted on a three-term fixture where FP32 addition is not associative, end to end, with each term produced by a different group. The ledger is charged **before a byte exists**. The sweep prints its coverage — **5,184 combinations**, 1,348 planned (224 all-device, 1,120 all-host, 4 mixed), 3,836 refused, every rejection reason exercised — and its strength is **measured**: 16 deliberate mutations, **16 caught**, 0 survivors, after two gaps the first measurement found ([experiment 0002](experiments/0002-expert-plan-sweep-mutations.md)). NUMA placement is **read back**, not asserted: with `required` it is `mbind` and **6,144 of 6,144** pages land on the planned node; with `auto` it is a preference whose result is printed. Three mechanisms failed first, each caught by measurement — an elided zero store, allocator-reused pages already faulted elsewhere (**3,317 of 8,192** on the wrong node), and the default policy falling back rather than reclaiming (**4,471 of 6,144**). **No performance claim**: no baseline exists on this machine |
| `G-EXPERT-PLAN-DEVICE` | `cargo test -p moxie-executor --features driver --test grouped_device -- --nocapture`; `cargo test -p moxie-executor --features driver --test grouped_real_artifact -- --nocapture`; `cargo xtask-cuda test-gpu` (`grouped_expert_mlp`) | device | task 0021; the **grouped expert kernel**, on all three UUIDs. Two symbols, one per gate transform, selected by the pure planner from an injected catalogue on operation, operand roles, precisions, rounding, layout, SM and shape — never a model name. **Bitwise** equal to task 0019's oracle: **36,864** BF16 components across three devices and both gate transforms, plus **45,056** more against the host candidate on real weights. `__fmul_rn`/`__fadd_rn` and `__dmul_rn`/`__dadd_rn` throughout, because nvcc contracts into FMA by default and that is a different answer. Weights are addressed straight out of a residency lease — no second cache — and the readback is per slot so the host groups' results survive. A mixed plan puts the two reused experts on the GPU and the three singletons on the CPU and reduces both in one declared order. A device cache holding **two** experts of ten executes under backpressure to the same answer. **One declared uncertainty is untested**: CUDA's FP64 `tanh`/`exp` may differ from the host libm's by under an ulp, and no fixture constructs a case where that straddles a BF16 tie point; it is covered contractually by `expert_error_bound` and is **not shown absent**. **No model, no quality claim** |
| `G-TOPOLOGY` | `cargo xtask test-topology` | device | **not implemented** (M5) |
| `G-QUALITY` | `cargo xtask quality` | device | **not implemented** (M3) |
| `G-BENCH` | `cargo xtask bench` | device | **not implemented** (M6) |

Device gates ran by hand on this machine. No CI runner executes them; see
[toolchain.md](toolchain.md).
Device results were remeasured in full at [task 0007](../tasks/0007-m1-rank-context-and-measured-capacity.md),
which changed the context type every device case uses.

Current workspace counts include [task 0015](../tasks/0015-m1-generation-service-diagnostic-cli.md) on 2026-09-11; historical task-specific counts and allocation envelopes retain their source scope. The M0
enforcement counts they grew from are in
[task 0002](../tasks/0002-m0-review-and-integer-transition.md#fourth-review-corrections-2026-09-07).

The owner accepted task 0010's bounded arena slice at `0cc61c7` after
[independent re-review](../handovers/2026-09-09-task0010-correction-review.md).
`G-ARENA-HISTORY` and `G-ARENA-PENDING` close the two original findings. This
acceptance remains unchanged. The owner accepted task 0011's admitted graph/resource-plan slice at
`64899c2` after independent re-review of `8b2fbd2..4ed80be`; no new actionable finding surfaced.
The review independently repeated the host workspace, focused executor driver/fault/unwind and
compile-fail checks on all three GPUs, device clippy, architecture, specification, formatting and
diff gates. It did not independently repeat the full device workspace, aggregate GPU
qualification, visibility/capacity matrix or isolated no-driver build; those remain separately
recorded implementation evidence. [Task 0012](../tasks/0012-m1-selected-bf16-device-chain.md)
implements semantic kernel selection and execution as the **final planned M1.3 closure task** at
`6305f9d`; all three owner-review findings are corrected at `eaf8846`, with the affected GPU, fault,
resource and sanitizer evidence passing. The two further findings are corrected at `1138a2a`.
Task 0012 closed M1.3. Tasks 0013 and 0014 add the accepted paged-state and
transactional sampler slices; accepted task 0015 adds their shared service/CLI
integration and closes M1.4. Task 0016 opens M1.5 with the reduced Gemma graph,
accepted [task 0017](../tasks/0017-m4-per-layer-kv-geometry-and-window-eviction.md)
gives the paged store per-layer key/value geometry and window reclamation, and
accepted [task 0018](../tasks/0018-m3-compressed-tensors-int8-importer.md)
imports the Gemma 4 artifact's real INT8 weights to canonical form.

**The owner closed M1.5, and with it M1, on 2026-09-12; M2 is active.** The
closure is on the reduced graph plus the importer, with the remaining blockers
recorded: the artifact is 32.7 GiB against a 24 GiB largest single GPU, so
executing it needs M3's W8A16 path *and* either M5's TP2 or M2's host-backed
residency. See [the closure handover](../handovers/2026-09-12-m1-closure-to-m2.md).
**Nothing in this matrix is a checkpoint execution or a quality claim.**

## Rows

| Checkpoint + artifact | Hardware / topology | Actual context | Feature / combination | Status | Test / benchmark / quality IDs | Limit or fallback |
|---|---|---|---|---|---|---|
| none | sm_86, sm_120 | n/a | BF16 host rounding contract | passed | `G-HOST-TEST` (`moxie-format::bf16`, exhaustive over all 65,536 patterns), `G-GPU-ALL` (`bf16_round_trip`) | Host oracle and device conversion agree bit for bit. Says nothing about a BF16 *model*. |
| none | n/a | n/a | INT4 affine host decode | passed | `G-HOST-TEST` (`moxie-format::affine`, all 16 codes, rebias, zero points, groups 32/128, tails, activation order) | Host reconstruction only. No kernel, no importer, no checkpoint. |
| none | n/a | n/a | INT8 affine host decode | passed | `G-HOST-TEST` (`moxie-format::affine`, all 256 codes including -128) | As above. |
| none | n/a | n/a | W4A16 / W8A16 execution | **not implemented** | — | M3. `ExecutionProfile` names the operand widths and declares that the weights must be dequantized; no kernel exists. Task 0018 imports the weights; nothing executes them. |
| **inspected, not executed** — `/fast/models/cyankiwi/gemma-4-31B-it-AWQ-8bit` rev `34ca187d` | n/a — host import | n/a | compressed-tensors `pack-quantized` INT8/INT4 **import** to canonical affine form | **accepted task 0018, 2026-09-12** | `G-CT-IMPORT` | **Import only. This is not model support and nothing executes.** Symmetric sources; asymmetric is an explicit typed refusal because no pinned exporter or local artifact establishes its zero-point serialization. The lane order within a packed word is taken from the pinned reader and **cannot** be verified against this artifact, because all four lanes fall inside one scale group — closing it needs paired output against the released model (**O2**). No quality claim follows. Nothing was written under any checkpoint root (**O5**). |
| **inspected and read, not executed** — `/fast/models/google/gemma-4-26B-A4B-it` rev `4d7ae498` | host cache + sm_86 / sm_120 device caches | n/a | Weight **residency**: bounded ranged reads, host cache, device upload readiness, leases, eviction, demand/prefetch classes | **accepted task 0020, 2026-09-12** | `G-RESIDENCY-HOST`, `G-RESIDENCY-DEVICE` | **Residency is not execution**, and this row is about residency only. Nine experts' bytes were demand-loaded and verified against an independent read; task 0021's row above is where any of them were computed with. The selected BF16 chain still refuses `Route`/`ExpertMlp`/`Combine`, which are lowered by `moxie_plan::expert` instead. M2's exit needs byte/cost traces reconciled with the ledger across a whole working set, not one layer. This row's device evidence is on synthetic weight-shaped bytes, not on this artifact. No quality claim (**O2**); nothing written under any checkpoint root (**O5**). |
| **executed on synthetic input** — `/fast/models/google/gemma-4-26B-A4B-it` rev `4d7ae498` | host + sm_86 / sm_120 | one layer's routed expert block, 2 rows of top-k 8 | Grouped expert execution: CPU fallback and GPU candidate plans under one interface, bounded queues, NUMA-aware host placement | **implemented task 0021, 2026-09-12; awaiting review and acceptance** | `G-EXPERT-PLAN-HOST`, `G-EXPERT-PLAN-DEVICE`, `G-RESIDENCY-HOST`, `G-RESIDENCY-DEVICE` | **Not model support.** 118,947,840 B of layer 0's experts were demand-loaded into a two-expert device cache and **computed with** — 28 evictions, 8 backpressure drains, and 45,056 BF16 slot components identical between the two candidates — but **the activations are synthetic and the route is written by the test**, so nothing here says anything about output quality (**O2**). One layer is not a model: nothing composes a routed layer into a graph that generates a token. The declared amortisation threshold is a **policy parameter, not a measured crossover**, and at this artifact's 11,894,784-byte experts and a two-row batch the default sends every expert to the CPU. Expert partitioning across the three cards is **M5**; `Route` and `Combine` have no device kernel. Nothing written under any checkpoint root (**O5**). |
| none | n/a | n/a | Canonical ranged reads + per-chunk checksums | **not implemented** | — | M3 repacker. Task 0020 narrowing 1: only `Shard::read_tensor_range` exists; `Artifact::read_tensor_range` refused — whole-tensor checksum unverifiable on a range. |
| none | n/a | n/a | Prepared weight layouts (`Preparing/Prepared*`) | **not implemented** | — | M3 repacker. Task 0020 narrowing 2: only `PreparedId` identity exists; states unimplemented — nothing prepares a layout yet, unreachable state would be a stub. |
| none | n/a | n/a | Prefetch predictor / measured transfer-vs-compute crossover | **not implemented** | — | M6. Task 0020 narrowing 3 + task 0021 policy: prefetch class/queue/ordering exist with explicit lists; `max_transfer_bytes_per_row` is a declared parameter, measured crossover unmeasured. Doc 03 allows smarter policy only with replayable traces + measured benefit. |
| none | n/a | n/a | AutoRound / AutoGPTQ packing import | **not implemented** | — | M3, second importer. |
| none | n/a | n/a | NVFP4 / FP8 / W4A4 / W8A8 | **unsupported** | — | Deferred by [ADR 0003](../decisions/adr/0003-int4-int8-bf16-weight-family.md). Not a gap to close; a decision. |
| none | sm_86, sm_120 | n/a | BF16 host reference interpreter | passed | `G-INTERP-BF16` | Two synthetic graphs, eight operations, FP64-transcribed references within their declared bounds. **No checkpoint, no kernel, no device, no sampler.** Synthetic output is never model support (document 06 M1.5). |
| none | n/a | n/a | Attention numerical contract | **passed, data-dependent** | `G-INTERP-BF16` (`moxie-oracles::attention`) | The bound depends on the conditioning of `Q·K`, is deliberately weak where it cancels, and carries an additive underflow term for subnormal results. A kernel qualified against it must state its data's conditioning. Two earlier versions were disproven by review; see task 0003. |
| none | n/a | n/a | First / partial / later chunked prefill | **partial** | `G-INTERP-BF16` (`whole_and_chunked_prefill_agree`, every width 1..6, logits and KV compared) | Proven on the host reference only. No device path, no paged-attention integration, no long-context execution. |
| none | n/a | n/a | Flash / paged / streamed-page attention | **not implemented** | — | M4. `moxie-oracles::mask::attend_row` is an exact reference, not a path. |
| none | sm_86 x2, sm_120 | n/a | Event-retained transient uploads | passed, bounded task 0009 slice | `G-HOST-TEST` (retirement, returned sweeps, both-scope/tier bounds and failed settlement), `G-GPU-ALL` (real uploads, foreign-completion refusal), `G-LEASE-FAULTS` | One upload per reservation, host `Pageable` capacity and UUID-owned device `TransferStaging` charged before allocation. Only observed completion permits checked free; copy/record/free failures quarantine with charges retained. Retired source ownership returns to the caller; device allocation is freed. No allocator, persistent residency transfer, kernel-chain lease, multi-stream fan-in or model execution is implemented. |
| none | sm_86 x2, sm_120 | n/a | Admitted basic device arena | passed, bounded task 0010 slice | `G-HOST-TEST` (`moxie-memory::arena` exact ranges/generations and `moxie-executor::arena` completion/cancel/sweep), `G-GPU-ALL` (`admitted_device_arena` on all 3 UUIDs), `G-LEASE-FAULTS` (real allocation/copy/record/free branches) | One physical allocation per reservation, deterministic aligned first-fit ranges, explicit release/coalescing, persistent owner transfer, and event-gated upload reuse. Copy/record/final-free ambiguity quarantines physical bytes and the parent charge. The basic slice permits one retained host source per arena and one completion event per use. No eviction, residency lifecycle, multi-stream fan-in, plan lowering, kernel chain or model execution. |
| none | sm_86 x2, sm_120 | n/a | Admitted graph/resource plan | **passed, bounded task 0011 slice** | `G-HOST-TEST` (`moxie-graph` graph identity and explicit index encoding, `moxie-plan` exact lowering/liveness/reuse/external-operand refusals, `moxie-executor::plan` exact ledger envelope and immutable-handle compile failure), `G-HOST-ARCH` (pure planner boundary), `G-RESOURCE-PLAN`, `G-LEASE-FAULTS`; owner acceptance at `64899c2` after independent re-review | A synthetic stateless graph and one UUID-bound row bucket lower to checked shapes, bytes, lifetimes, layouts and deterministic arena slots. External indices explicitly retain the supported `U64` encoding and checked byte extent; weight-role per-step inputs are refused, and the shared ledger admits one activation arena plus exact declared BF16 weight requirements. Read-only handles bind checked descriptors to allocation identity. Execution always refuses with `UnsupportedKernel`. No weight is loaded or resident, and no state, sampling, generation, semantic kernel, model or performance is supported. |
| none | sm_86 x2, sm_120 | n/a | Selected BF16 Linear → RmsNorm → Residual device chain | **accepted task 0012; M1.3 complete** | `G-BF16-DEVICE-CHAIN`, `G-LEASE-FAULTS`, `G-HOST-ARCH`; source `6305f9d`, corrections `eaf8846` and `1138a2a` | Closed SM86/SM120 descriptors select exact stateless H8 rows1 and H17 rows5 graphs. One admitted allocation contains separately charged weights, activations and RMS workspace; one stream retains uploads/ranges through one event and performs one final readback. The readback's host storage overlaps retained sources in admission. Invalid RMS intermediates fail numerically after safe completion, and completed cancellation recovery preserves immutable weights. Every-node representable results are bit-identical to the interpreter and general results satisfy the fixed bounds. This qualifies only these synthetic BF16 operations/shapes and primitive bounds through rows64/H1024. **No checkpoint/model, attention/state, actual context, generation, quality or performance is qualified.** |
| none | n/a | n/a | Oversized host/disk weight streaming | **not implemented** | — | M2. The accounting core, transient leases and one basic device arena exist, but no residency lifecycle, host cache, demand readiness, eviction or storage integration does. |
| n/a | n/a | 2 nodes, 56 threads | **NUMA-aware host placement** of a plan's own buffers | **implemented task 0021, 2026-09-12; awaiting review and acceptance** | `G-EXPERT-PLAN-HOST` (`host_buffers_are_bound_to_the_planned_node_and_the_pages_are_read_back`), `G-HOST-TEST` (`moxie-host::numa`) | Topology **measured** from sysfs, never assumed: node 0 carries the 5060 Ti, node 1 the 3090 pair, reproducing `hardware-inventory.md` rather than trusting it. A device the kernel gives no node stays `Unspecified`. `required` binds pages with `mbind` and every page is verified through `numa_maps`; `auto` is a preference whose outcome is reported. **The residency authority's own host cache is NOT placed** — it is task 0020's buffer and the largest host buffer in the system. Placement changes no result and no number here is a throughput claim. |
| none | n/a | n/a | TP / PP / expert partition | **not implemented** | — | M5. `PartitionRule::NotDetermined` fails closed today. |
| none | n/a | n/a | Transactional publication of a step | passed | `G-INTERP-BF16` (`moxie-state` `begin`/`commit_prefix`/`abort`, `a_step_cancelled_at_any_publication_boundary_aborts_to_exactly_where_it_started`) | Host reference only. A step's sequence-state and KV mutations commit together or restore exactly, and a transaction is append-only on both. `fork` is still identity-only (COW is M4), and accepted task 0013 separately binds physical host pages to the same sequence journal. |
| none | host | 32,768 stored rows; no attention | Appendable paged KV state | **accepted task 0013** | `G-PAGED-HOST`, `G-PAGED-ALLOCATION` | One root branch; fixed pool admitted by the shared ledger. BF16/FP16/FP32 byte preservation, partial pages and cancellation/rollback. Post-admission lineage exhaustion is typed capacity failure with complete release. Pool remains fully charged until close. No COW, dynamic growth, sliding reclamation, device attention, sampler or model support. |
| none | n/a | n/a | Canonical manifest v1 + bounded tensor reads | passed | `G-HOST-TEST` (`moxie-format::manifest` 31 rejection/limit/collision tests, `moxie-storage` 15 artifact + 8 pump tests), `G-HOST-ARCH` (format/storage boundary + include/module rules with fixtures, incl. positive layouts) | Host-only. Opens, validates and reads test-generated BF16 artifacts within a byte budget; chunk files are opened once and read position-independently, and the success path holds zero live heap on short and long paths alike (counting-allocator gate). **No checkpoint has been read, downloaded or converted, and nothing here loads a model:** `moxie-interp` is not rewired to disk, affine tensors refuse with M3, and `partial` refuses every read. |
| none | n/a | n/a | Resource ledger, admission and refusal | passed | `G-HOST-TEST` (`moxie-memory` 42 admission acceptance + 24 unit tests + 3 compile-fail doctests; `moxie-types::tier` and `::ids` exhaustiveness, identity and malformed-input tests), `G-HOST-ARCH` (memory/filesystem and memory/model-name boundary rules, each with a rejecting fixture and a clean accepted one) | Host-only integer accounting over byte counts the caller declares. **The ledger allocates no payload, maps nothing, measures nothing, and names no model.** Capacity comes in as a snapshot measured elsewhere; admission reserves the peak overlapping live set atomically, or refuses with a per-tier breakdown and only alternatives recomputation shows can move a failed constraint. `moxie-memory::Arena` owns pure range metadata; transient uploads and the basic device arena are separate physical consumers. Task 0013 adds `HostBuffer` for admitted synchronous host backing, consumed by paged state. |
| none | n/a | n/a | Derived engine/host reserves | **partial** | `G-GPU-CAPACITY` | Working placeholder constants `ENGINE_RESERVE_BYTES` 64 MiB (0007) + `HOST_RESERVE_BYTES` 8 GiB (0008), named and documented. Document 03 requires derived reservations, not copied constants; replacement is M6 admission refinement. |
| none | this host | n/a | Measured host capacity | passed | `G-GPU-CAPACITY`, `G-HOST-TEST` (`moxie-host` 17 tests over committed fixture trees — cgroup limits on a leaf, on an ancestor, leaf-tighter, and sibling-pressure where total and headroom bind at different levels, `max`, no hierarchy, cgroup v1, large swap, coherence under both the machine view and a cap, and seven refusals including missing usage with a finite limit, a malformed limit, and unreadable/non-UTF-8 membership; `moxie-memory::snapshot::measured_host` rules), `G-HOST-ARCH` (telemetry outside `moxie-host` refused in absolute and relative `proc`/`sys` spellings with an artifact-path accepted counterpart, and a `moxie-memory` -> `moxie-host` dependency refused) | `MemAvailable` after the smallest cgroup v2 limit for total and the smallest headroom for available, minimised independently over the process's chain, less a declared reserve. **Nothing is allocated, mapped or reclaimed.** **Swap is excluded from every budget on purpose** (document 03) and reported so the exclusion is visible; page-cache pressure is reported, not managed — managing it is M2. A reading is not a reservation: another process can take memory a moment later and the snapshot will not know, and no pressure-driven replan exists. On this machine no cgroup limit applies, so the limited paths are proven by fixtures rather than by the machine. |
| none | sm_86 x2, sm_120 | n/a | Measured device capacity, and rank-owned contexts | passed | `G-GPU-CAPACITY`, `G-GPU-ALL` (`rank_context_is_exclusive`, `concurrent_handoff_is_exclusive`, `measurement_is_live`), `G-HOST-TEST` (`moxie-cuda::claims`, which proves the claim outlives the driver teardown without a GPU present), `G-HOST-TEST` (`moxie-memory::snapshot::measured` rules on synthetic readings), `G-HOST-ARCH` (a `moxie-cuda` -> `moxie-memory` dependency is refused) | One rank owns one GPU, and a device's free/total memory reaches the ledger as a `CapacitySnapshot` keyed by UUID. Transient uploads and the bounded device arena consume admitted budgets (tasks 0009–0010). A measurement is a reading at one instant, not a reservation: another process can take memory a moment later and the snapshot will not know. Host capacity is measured too. The figures are byte counts of capacity and are not performance results (O6 untouched). |
| none | n/a | n/a | Prefix reuse / COW state branches | **not implemented** | `G-HOST-TEST` (`moxie-state`) covers frontier, result-identity and restore-evidence rules only | M4. Counters, retained-result identity and prefix lineage are typed and tested; the new host page pool has no COW/prefix-sharing integration, and document 04's token-content prefix key is a separate identity that is **not** implemented. |
| none | host | 32,768 history entries; no attention | Base sampler and transactional history | **accepted task 0014, 2026-09-11** | `G-SAMPLING-HOST`, `G-SAMPLING-ALLOCATION`, `G-HOST-ARCH` | One generation with fixed capacity, FP64 greedy/temperature distributions, explicit legality, Philox target draws, generated-token positions and tentative/committed counts. Shared journal restores rows/frontiers/history; replay evidence follows actual count rebuilding. No model-output provenance, GPU sampling, service/CLI, speculation or entropy integration. |
| none | host-reference synthetic | <=256 requested context; no checkpoint | Shared generation service and diagnostic CLI | **accepted task 0015; M1.4 complete** | `G-GENERATION-HOST`, `G-GENERATION-ALLOC`, `G-HOST-ARCH` | Typed pull events, chunked prefill, one-token decode, accepted sampler and paged state; exact dense-reference parity for two shapes. Every executed prefill/tail/decode row shape is admitted up front, one immutable program owns each paged history, and forward payload allocation failure aborts all work in the open transaction with complete cleanup/retry. No tokenizer, text stop/EOS, HTTP, checkpoint, GPU attention or production CPU fallback. |
| **none** — synthetic BF16 weights | host-reference synthetic | <=256 requested context; no checkpoint | Reduced Gemma-4-like dense text graph | **accepted task 0016, 2026-09-12; extended by task 0017; M1.5 active** | `G-GEMMA-REDUCED`, `G-KV-RETENTION`, `G-GENERATION-HOST`, `G-GENERATION-ALLOC`, `G-HOST-ARCH` | **This is not Gemma support and its output is not model output.** Two reduced geometries compose sliding/global layer types at **their own key/value head counts and head dimensions**, per-layer-type RoPE, grouped-query attention at score scale 1.0, per-head Q/K norm with unit-gain V norm, global layers whose V comes from the key projection, GeGLU, per-layer MLP residual scalars, an embedding scale, tied embeddings and a logit softcap — all through shared operations with registered oracles. Sliding layers physically reclaim outside their window and refuse a reclaimed read. Still reduced, each with its owner: synthetic BF16 weights because the artifact is INT8 `pack-quantized` (**M3**), text only (**M11**). The CLI prints that list before any output. |
| **none** — synthetic BF16 weights | host-reference synthetic | reduced expert counts; no checkpoint | Routed-expert semantics: `Route` -> `ExpertMlp` -> `Combine` | **accepted task 0019, 2026-09-12; M2 active, items 2-5 outstanding** | `G-MOE-ROUTING-HOST`, `G-GEMMA-REDUCED`, `G-GENERATION-HOST`, `G-HOST-ARCH` | **This is not Gemma 4 MoE support and its output is not model output.** Two independent consumers: a Gemma-4-MoE-like block with a dense shared expert beside routed experts, a router reading the **un-normalized** residual, three extra norms per layer and a per-expert coefficient scale; and a synthetic MoE with a different expert count, `top_k == experts`, SwiGLU, no shared expert, no per-expert scale and selection-order combination. The designated artifact (`/fast/models/google/gemma-4-26B-A4B-it`, 51.6 GB BF16, 128 experts at top-k 8) was **inspected and not executed**: it fits aggregate VRAM and no single 24 GiB device. Outstanding, each with its owner: the residency authority, expert chunk identity, demand cache, eviction and CPU fallback (**M2 items 2-3, task 0020**); grouped GPU expert execution and CPU fallback (**M2 item 3**); expert partitioning (**M5**) and shared performance work (**M6**); quality against the released model (**O2**); vision and audio (**M11**). One declared numerical deviation from the pinned reference remains — FP32 accumulation of the `top_k` terms against its per-term BF16 narrowing — and is **not** covered by the FP32 `metric::bound`; the correct statement and its counterexample are in the task record. Every other BF16 boundary the reference has is implemented, after an independent review found the router's and the expert's missing and a probe showed that omitting them changes which experts a row selects. |
| none | n/a | n/a | Per-layer paged key/value geometry and sliding-window reclamation | **accepted task 0017, 2026-09-12** | `G-KV-RETENTION`, `G-WINDOW-ALLOCATION`, `G-PAGED-HOST` | **Host storage only.** One ring per layer, reclamation by overwrite, bounded tentative-undo headroom ([ADR 0014](../decisions/adr/0014-bounded-tentative-undo-headroom.md)). No device paged attention, no COW fork, no host-backed page streaming, no recurrent or index state — the rest of **M4**. Not a context claim: 32,768 *stored rows* is storage evidence, not attention over 32,768 tokens. |
| none | n/a | n/a | Gemma 4 31B-IT checkpoint execution | **not implemented** | — | The artifact is inspected and hashed ([inventory](checkpoint-inventory.md), [bring-up record](../models/gemma4.md)) and nothing more. Every language-model linear is compressed-tensors INT8 `pack-quantized`; the importer, packed-layout reader, canonical repack and W8A16 path are all **M3**. O1, O2 and O5 remain open. |
| none | n/a | n/a | Complete common sampler pipeline | **partial** | `G-HOST-TEST` (`moxie-oracles::sampler`), `G-SAMPLING-HOST` | Base legality/greedy/temperature/RNG/history are implemented under task 0014. Top-k/top-p/min-p remain oracle-only. Penalties, DRY, n-gram ban, logit bias, typical-p and XTC are **not implemented**. |
| none | n/a | n/a | Future entropy | **not implemented** | — | M10. Document 05 defines it; nothing here evaluates it. |
| none | n/a | n/a | Speculation, any proposer | **not implemented** | `G-HOST-TEST` (`moxie-state`) covers acceptance-frontier bookkeeping only | M9. No verifier, no proposer. |
| none | n/a | n/a | HTTP / CLI / SSE / reasoning / tools / images | **not implemented** | `G-HOST-TEST` (`moxie-oracles::protocol`) covers frame order, usage arithmetic and stop-string holding | M8. There is no server and no client. |
| none | n/a | n/a | Long context (32,768 and above) | **unmeasured** | — | M4 gate. No inference has run at any context length. |
| none | n/a | n/a | Throughput, any phase | **unmeasured** | — | M6. No benchmark has run. |
| none | n/a | n/a | Measured transfer/storage cost model (pinned vs pageable, simultaneous transfers, D2H/peer BW, per-mount sustained read, thermal/power, host pinned limits) | **not implemented** | — | M5 cost probes (+ M0 follow-up named in `hardware-inventory.md:119-124`). Bounded H2D probes + P2P DMA-vs-kernel costing exist; planner-usable simultaneous/pinned/sustained figures do not. Per-mount sustained read explicitly unmeasured; required before any disk-spilling claim. |

## What the passing rows do not mean

- A passing host decode gate means the reconstruction equation is right for synthetic tensors. It
  does not mean any of the ten [candidate checkpoints](quantization-candidates.md) can be loaded:
  their tensor headers, zero-point packing, activation-order maps and exclusion lists are
  uninspected at the byte level.
- The task 0005 smoke cases remain toolchain probes. `G-BF16-DEVICE-CHAIN` additionally qualifies
  the narrowly named synthetic BF16 operations, shapes and SM images above. It does not qualify a
  checkpoint, model graph, attention/state path, actual context, generation, quality or throughput.
- A passing capacity gate means this machine and three GPUs answered a question about themselves and
  the ledger admitted a plan against the answers. Task 0010 separately proves one small admitted
  device arena allocates; the capacity probe itself still allocates no payload and proves no plan
  execution. Either capacity figure can change a second later. The host figure in particular is
  `MemAvailable`, which includes cache the kernel expects to reclaim.
- A passing ledger gate means the accounting arithmetic and refusal rules are right for supplied byte
  counts. The selected-chain gate derives and admits one synthetic graph's exact weight,
  activation, workspace, retained upload-source and final-output envelope and materializes the
  device ranges in one allocation. It has no reusable residency pipeline, host cache, state,
  attention or model run.
- `G-HOST-NODRIVER` means host CI can run anywhere. It is not evidence about any device, and
  document 07 forbids reading it as such.
